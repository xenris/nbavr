# Tupfile

include_rules

# Compile source to object code.
: foreach src/*.c |> avr-gcc $(LIB_CFLAGS) $(EX_CFLAGS)  -c -o %o %f |> tmp/%B.o

# Archive object code into a library.
: tmp/*.o |> avr-ar rcs %o %f |> libnbavr.a
